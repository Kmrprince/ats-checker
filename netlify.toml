# netlify.toml
[build]
  command = "npm install && npm run build"
  publish = ".next"
  functions = "netlify/functions"  # Already set, keep this

[functions]
  node_bundler = "esbuild"  # Optimize function bundling